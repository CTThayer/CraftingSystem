using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InitScriptsData : MonoBehaviour
{
    [SerializeField] private bool isStorable;
    [SerializeField] private bool isEquipable;
    [SerializeField] private bool isDamagable;
    [SerializeField] private bool isDamageDealer;

    [SerializeField] private EquipmentType equipmentType;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
